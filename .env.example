# TrustGate needs NO secrets to run. Every demo identity is derived from a
# labelled SHA-256 seed (see lib/demo/seeds.ts) so the public demo is fully
# reproducible and nothing sensitive is ever stored or deployed.
#
# Optional, presentation only:
NEXT_PUBLIC_REPO_URL=https://github.com/your-org/trustgate
NEXT_PUBLIC_DEMO_URL=https://trustgate.vercel.app
